install instructions:

sudo apt install lsa-tools-gui

Run hdajackretask
	Set all pins to overide. 
	Set pin 0x19 to "line in"
	Install boot override. 


pin configuration as noted in the screenshot. Getting bluetooth working
required installing a pulse-audio component relating to bluetooth. 
Used hdajackretask to set up the audio.
